using UnityEngine;

public class SumoManager : MonoBehaviour {
    public static SumoManager Instance { get; private set; }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public int Test() {
        return 1;
    }
}
